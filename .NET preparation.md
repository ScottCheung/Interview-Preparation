<!-- @format -->

# 📚 主题一：Hosting、启动流程与环境配置

从最基础的程序入口、初始化组件开始，逐步深入到配置加载、环境隔离以及底层服务器模型。

### 🟢 基础（初级）

1. `Program.cs` 在 .NET 6+ 中发生了什么变化？什么是 top-level statements（顶级语句）？
2. `WebApplicationBuilder` 是做什么的？`builder.Services` 和 `app` 的区别是什么？
3. 传统旧版本中的 `ConfigureServices` 和 `Configure` 方法在 .NET 6+ 后变成了什么？
4. `Development`（开发）和 `Production`（生产）环境有什么区别？ASP.NET Core 如何识别当前环境？
5. `launchSettings.json` 有什么作用？它会部署到生产环境吗？

### 🟡 进阶（中级）

6. `appsettings.json` 是如何自动加载的？如果不同环境的配置文件有冲突，其覆盖顺序（Precedence）是怎样的？
7. ASP.NET Core 如何读取操作系统环境变量？它与 JSON 配置文件的键如何建立映射？
8. 什么是 Options Pattern（选项模式）？你为什么会使用 strongly typed configuration（强类型配置）？
9. `IOptions<T>`、`IOptionsSnapshot<T>` 和 `IOptionsMonitor<T>` 的生命周期和区别是什么？
10. 什么是 Secret Manager（机密管理器）？为什么不能把 Connection String（数据库连接字符串）等敏感信息写死在代码或基础 JSON 中？在生产环境应该如何安全地隐藏和管理这些敏感配置？
11. 什么是 Kestrel？它是一个什么样的 Web 服务器？
12. Kestrel 和 IIS（或者 Nginx、Apache）的关系是什么？为什么要使用反向代理（Reverse Proxy）？IIS 的 `InProcess` 和 `OutOfProcess` 模式有什么区别？

### 🔴 深度（高级）

13. ASP.NET Core 的 hosting model（宿主模型）有哪些？从 `.NET Core 1.x` 到 `.NET 8/9` 宿主构造器经历了怎样的演变（从 `WebHostBuilder` 到 `GenericBuilder` 再到 `WebApplication`）？
14. 深入解释配置系统的底层机制：`IConfiguration` 内部是如何工作的？它是如何将多个不同的配置源（Json、EnvironmentVariables、CommandLine）融合成一个统一的键值对树状结构的？
15. Kestrel 的线程模型是怎样的？在底层它是如何利用 `Transport`（如 Socket 传输层或 Linux 上的 epoll）来接收并分发 HTTP 请求的？
16. ASP.NET Core 是如何做到真正的跨平台的？其底层的运行时（CLR）、.NET Standard 以及平台抽象层（PAL）起到了什么作用？

---

# ⚙️ 主题二：Request Pipeline（请求管道）与 Middleware（中间件）

探讨请求从进入服务器到返回响应的完整生命周期。这是 ASP.NET Core 架构的灵魂。

### 🟢 基础（初级）

17. ASP.NET Core 的 Request Pipeline（请求管道）是怎么工作的？请画出或描述其“俄罗斯套娃”/“双向洋葱圈”模型。
18. 什么是 Middleware（中间件）？它和传统 ASP.NET 的 `HttpModule` / `HttpHandler` 有什么区别？
19. `app.Use` 和 `app.Run` 的区别是什么？
20. `app.Run` 为什么会终止管道？其底层原理是什么？
21. 为什么 Middleware 的执行顺序极其重要？如果把 `UseAuthorization()` 放在 `UseRouting()` 前面会发生什么？

### 🟡 进阶（中级）

22. 什么是 Middleware Short-Circuiting（中间件短路）？在什么业务场景下我们会主动让管道短路？
23. 如何编写一个自定义的中间件？基于约定（Convention-based）和基于工厂（Factory-based, `IMiddleware`）的中间件有什么区别？
24. `HTTPS Redirection Middleware` 和 `Forwarded Headers Middleware` 是做什么的？
25. 当 API 部署在 Nginx/Reverse Proxy 后面时，ASP.NET Core 如何正确识别 Client IP 和原始请求协议（HTTP/HTTPS）？
26. 为什么全局异常处理中间件（Exception Middleware）比在每个 Controller 里写 `try-catch` 更好？你如何实现统一的错误返回？
27. 你如何使用中间件来实现 Request Logging（请求日志记录）和追踪 `Request ID`（Correlation ID）？

### 🔴 深度（高级）

28. **【核心源码级】** ASP.NET Core 中 Request Pipeline 是如何被构建成一个 Delegate Chain（委托链）的？`UseMiddleware` 的内部实现原理是什么？
29. 中间件里的 `RequestDelegate` 和 `HttpContext` 是什么关系？`app.Use(async (context, next) => { ... })` 中的 `next` 代表了什么？
30. 中间件可以被并行执行吗？为什么？Request Delegate 在构建完成后可以被缓存复用吗？
31. 深入异步机制：`async/await` 在 ASP.NET Core Pipeline 中如何运行？`SynchronizationContext` 在 .NET Core / .NET 6+ 中还存在吗？这对线程切换有什么影响？
32. `HttpContext` 生命周期是什么？它是线程安全的吗？为什么不能在异步操作（如未等待的 Background Task）之后错误地使用或传递 `HttpContext`？
33. `IHttpContextAccessor` 是如何工作的？为什么利用 `AsyncLocal<T>` 可以在异步上下文中跨线程追踪 `HttpContext`？它的性能损耗在哪里？

---

# 💉 主题三：Dependency Injection（依赖注入）

ASP.NET Core 的一等公民，也是高频面试考点。包含生命周期、陷阱及底层原理。

### 🟢 基础（初级）

34. 什么是 Dependency Injection Container（依赖注入容器 / DI 容器）？
35. ASP.NET Core 依赖注入中，`Transient`、`Scoped`、`Singleton` 三大生命周期的区别是什么？请分别举出适合它们的业务场景。
36. 为什么 ASP.NET Core 默认且强烈推荐使用 Constructor Injection（构造函数注入）？
37. `IServiceCollection` 和 `IServiceProvider` 的区别与联系是什么？

### 🟡 进阶（中级）

38. 为什么 `DbContext`（如 Entity Framework Core 里的上下文）不能或者不推荐注册成 `Singleton`？
39. 为什么 `Singleton` 服务里面不要保存和 Request 相关的状态（Request State）？
40. 什么是 Captive Dependency（捕获依赖）错误？即如果一个 `Singleton` 服务注入了一个 `Scoped` 服务，会发生什么严重后果？
41. 什么是 Service Locator Anti-Pattern（服务定位器反模式）？为什么在代码中频繁直接调用 `Context.RequestServices.GetService<T>()` 是不好的设计？
42. 什么是 Circular Dependency（循环依赖）？在运行时它是如何被检测出来的？如果你遇到了 DI 循环依赖问题，应该如何优雅地重构或解决？

### 🔴 深度（高级）

43. `IServiceScopeFactory` 是用来干什么的？如何安全地在 `Singleton` 服务（例如一个长时间运行的 `BackgroundService`）中使用 `Scoped` 服务？请演示如何手动创建 `IServiceScope`。
44. 当一个 `IServiceScope` 被 `Dispose` 时，其内部解析出来的各种生命周期的服务在内部是如何被释放的？ASP.NET Core 如何防止服务泄漏（Service Leak）？
45. **【底层原理】** 依赖注入容器内部是如何解析依赖的？它是否支持表达式树（Expression Tree）生成来优化实例化性能？
46. ASP.NET Core 内建 DI 的优缺点是什么？它为什么不支持属性注入（Property Injection）和方法注入？为什么不支持拦截器（Interception/AOP）？如果项目需要这些高级特性，应该如何集成第三方容器（如 Autofac）？
47. 在 Build Service Provider 阶段，ASP.NET Core 是如何进行服务生命周期验证（Service Scope Validation）的？如何开启它？

---

# 🛣️ 主题四：Routing（路由）与 Minimal API

探讨请求是如何精准匹配到对应的处理端点，以及新一代 Minimal API 的演进。

### 🟢 基础（初级）

48. 什么是 Endpoint Routing（端点路由）？
49. `UseRouting` 和 `UseEndpoints` 的职责分别是什么？它们在管道中是如何分工的？
50. Conventional Routing（约定路由/常规路由）和 Attribute Routing（特性路由）的区别是什么？为什么 Attribute Routing 更适合 Web API？
51. `app.MapControllers()` / `MapControllers` 内部做了什么？

### 🟡 进阶（中级）

52. Controller-based API 和 Minimal API 的区别是什么？
53. 在什么业务情况下或者出于什么考虑，你会选择使用 Minimal API 而不是传统的 Controller？
54. `MapGet()` / `MapPost()` 等 Minimal API 方法与 `MapControllers()` 在路由树的构建和内部底层实现上有何区别？
55. 什么是 Route Constraint（路由约束）？它是如何在 URL 层过滤不合法的请求的？ASP.NET Core 如何处理 URL 参数和复杂的 Query String？

### 🔴 深度（高级）

56. **【架构演进】** 深入探讨 Minimal API Pipeline 和 MVC Controller Pipeline 的本质不同：为什么 Minimal API 的执行速度更快、分配的内存更少？它跳过了 MVC 的哪些繁重的阶段？
57. 路由中间件的底层职责是什么？它是如何在 `HttpContext` 上附加 `Endpoint` 对象的？
58. `ActionSelector` 和路由匹配引擎是如何根据当前请求的 HTTP Method、URL 和 Accept Header 来在成百上千个路由中快速挑出正确的 Action 方法（ActionDescriptor）的？内部有何缓存机制？

---

# 🎮 主题五：MVC Pipeline & Model Binding & Validation

进入 Controller 内部，分析参数绑定、数据校验以及各种 Filter（过滤器）的切面编程。

### 🟢 基础（初级）

59. 描述一下 ASP.NET Core MVC Action Execution Pipeline（Action 执行管道）的完整生命周期。
60. ASP.NET Core 中 Model Binding（模型绑定）是怎么工作的？
61. 特性 `[FromBody]`、`[FromQuery]`、`[FromRoute]`、`[FromForm]`、`[Header]` 的区别和使用场景是什么？
62. 为什么在一个 Action 方法参数中，只能有一个参数标记为 `[FromBody]`？
63. `ModelState` 是什么？`ModelState.IsValid` 是如何自动触发或手动检查的？
64. `[ApiController]` 特性有什么作用？它为你省去了哪些样板代码（Sample Code）？

### 🟡 进阶（中级）

65. Middleware（中间件）和 Filter（过滤器）的区别是什么？什么时候应该用 Middleware，什么时候应该用 Filter？
66. 请列举并排序 ASP.NET Core 支持的 5 种 Filter 及其执行顺序（Authorization, Resource, Action, Exception, Result）。
67. `Authorization Filter` 是什么时候执行的？它能拿到 Action 的参数吗？
68. `Action Filter` （`IActionFilter` 与 `IAsyncActionFilter`）有什么作用？
69. `Exception Filter` 和 `Exception Middleware` 有什么层级和功能上的区别？你什么时候会选择用 Exception Filter？
70. `Result Filter` 的使用场景是什么？`IActionResult` 最终是如何被转换为 HTTP Response 的？
71. 传统的 Data Annotations（数据注解属性如 `[Required]`, `[StringLength]`）和第三方库 `FluentValidation` 的区别和优缺点是什么？

### 🔴 深度（高级）

72. **【源码级绑定流程】** Model Binding 的底层实现流程是什么？`IModelBinderProvider` 和 `IModelBinder` 是如何根据参数类型协同工作的？如何实现一个自定义的 ModelBinder？
73. 什么是 Binding Source Inference（绑定源推导）机制？
74. 当 Model Binding 失败时（例如传入了错误的日期格式或类型不匹配），Pipeline 是如何继续运行的？`ModelStateDictionary` 内部是如何记录这些错误的？
75. `ObjectResult` 和 `ContentNegotiation`（内容协商）是如何工作的？`Accept` 请求头是如何影响后端 Output Formatter（输出格式化器）的选择并返回 JSON 或 XML 的？
76. `Controller` 本身是如何被实例化的？`IControllerActivator` 起了什么作用？Controller 里的 Action Metadata 是如何被 MVC 框架缓存的？

---

# 📝 主题六：JSON 序列化、文件处理与大文件流

Web API 核心的数据交换媒介（System.Text.Json）与复杂的 I/O 读写。

### 🟢 基础（初级）

77. ASP.NET Core 默认的 JSON Serialization（序列化）是如何工作的？
78. `System.Text.Json` 和第三方的 `Newtonsoft.Json` (Json.NET) 有什么本质区别？为什么 .NET Core 3.0+ 要自研前者？
79. 在 Web API 中，CamelCase（驼峰命名）属性是如何自动启用的？Enum（枚举）默认序列化为数字还是字符串？如何更改它？
80. 什么是 JSON 序列化的循环引用（Circular Reference）问题？它是怎么发生的？
81. `[JsonIgnore]` 特性有什么作用？

### 🟡 进阶（中级）

82. 为什么 `DateTime` / `DateTimeOffset` 序列化在前后端交互和跨时区时容易出问题？为什么在生产环境中使用 UTC 时间非常重要？ASP.NET Core 如何统一处理时区问题？
83. 如何编写和注册一个自定义的 `JsonConverter<T>` 来满足特殊的格式需求？
84. ASP.NET Core 如何实现文件下载？`FileStreamResult`、`PhysicalFileResult`、`VirtualFileResult` 和 `FileContentResult` 的区别和适用场景是什么？
85. 面对超大文件上传（如几百 MB 或数 GB），你如何使用 `multipart/form-data` 来流式处理请求，而不是一次性读入内存导致崩溃？
86. 什么是 `CancellationToken`？它在 Web API Controller 的 Action 方法中有什么作用？
87. `HttpContext.RequestAborted` 是什么？为什么长时间运行的异步请求或大型数据库查询强烈建议传入 `CancellationToken`？

### 🔴 深度（高级）

88. **【高性能 I/O】** `System.Text.Json` 底层是如何做到高吞吐和 Zero Allocation（零分配）的？`Utf8JsonReader` 和 `Utf8JsonWriter` 为什么基于 `ReadOnlySpan<byte>` 而不是 `string`？
89. 深入探讨内存压力（Memory Pressure）与高性能：在 JSON 序列化和大文件处理中，ASP.NET Core 是如何利用 `ArrayPool<T>` 和 `MemoryPool<T>` 进行内存复用的？
90. 什么是大对象堆（LOH, Large Object Heap）碎片化？在 API 读写文件、序列化大型 JSON 对象时，如何设计才能避免触发 LOH 的频繁垃圾回收（GC）？
91. 什么是 Streaming JSON（流式 JSON 处理）？如何利用 `IAsyncEnumerable<T>` 实现服务器端向客户端边查边流式输出 JSON 数据？
92. 什么是 JSON Patch（RFC 6902）和 JSON Merge Patch？在 ASP.NET Core 中如何实现资源的部分更新（Partial Update）？

---

# 🛡️ 主题七：API 架构设计、安全、认证与授权

涵盖 RESTful 实践、Identity、JWT 安全体系、跨域以及高级网络防护。

### 🟢 基础（初级）

93. 你是如何设计一个成熟的 RESTful API 的？什么情况下应该返回 200, 201, 204, 400, 401, 403, 404, 500 等 HTTP 状态码？
94. 为什么不能在 API 中直接返回 Entity Framework 的 Entity（实体类）？为什么要使用 DTO（Data Transfer Object）？
95. 什么是 Over-posting Attack（过度提交攻击 / 批量赋值漏洞）？使用 DTO 为什么能防范它？
96. `IActionResult`、`ActionResult` 和 `ActionResult<T>` 的区别是什么？你倾向于用哪种？为什么？
97. 什么是 Authentication（认证）和 Authorization（授权）？它们的本质区别是什么？
98. 核心特性 `[Authorize]` 是如何工作的？

### 🟡 进阶（中级）

99. 什么是 CORS（跨域资源共享）？如果前端或移动端在调用 API 时出现 CORS Error，你的标准排查和修复步骤是什么？
100. 深入解释 JWT（JSON Web Token）的结构（Header, Payload, Signature）。为什么它如此适合无状态的 Web API？
101. JWT Token 通常存放在 HTTP 请求的什么地方？Authentication Middleware 是如何提取并验证它的？
102. 为什么在 JWT 体系中需要引入 Refresh Token？它是如何工作的？
103. 发生 JWT 秘钥被盗或需要强制用户强制下线（Token Revocation）时，你有哪些行之有效的架构解决方案？
104. 什么是基于角色（Role-based）、基于声明（Claim-based）和基于策略（Policy-based）的授权？如何在 ASP.NET Core 中配置它们？
105. 什么是 ProblemDetails（RFC 7807）规范？为什么 ASP.NET Core 默认的错误返回极力推荐使用它？
106. Swagger（OpenAPI 文档）是如何自动扫描并生成的？`Swashbuckle` 起了什么作用？如何让 Swagger 支持 JWT Bearer 认证的在线测试？如何隐藏某些内部 API 不显示在文档中？
107. API Versioning（API 版本控制）有哪些实现方式（URL 路由、Query String、Custom Header、Accept Header）？它们各自优缺点是什么？

### 🔴 深度（高级）

108. 深入剖析 JWT Validation Pipeline 的底层步骤：当请求携带 Token 进来，`JwtBearerHandler` 内部是如何使用配置的 `TokenValidationParameters` 去校验签名、过期时间、Issuer 和 Audience 的？
109. 什么是 Signing Key Rotation（签名密钥轮转）？在大型分布式系统或微服务中，API 如何通过 JWKS（JSON Web Key Sets）端点自动获取最新公钥来解密 Token？
110. 如何防范针对 API 的各种常见网络攻击？

- 如何防止 SQL Injection（SQL 注入）？
- 如何在 API 层防范 XSS（跨站脚本攻击）和 CSRF（跨站请求伪造）？
- 什么是 Token Replay Attack（Token 重放攻击），如何缓解？

111. 生产环境中，ASP.NET Core Web API 发生 Crash（进程崩溃）最常见的 Root Cause（根源）有哪些？当 API 彻底死锁或崩溃时，如何利用 dotnet-dump、dotnet-gcdump 等诊断工具采集 Dump 并分析？

---

# 🚀 主题八：高性能、异步、工程实践与性能调优

聚焦高级系统级优化：异步原理、连接池耗尽、并发、多线程、性能瓶颈及真实生产排查。

### 🟢 基础（初级）

112. 为什么 `async / await` 异步编程对高并发的 Web API 如此至关重要？它能减少单个请求的响应时间（Latency）吗？
113. 为什么绝对不要在异步 API 中阻塞调用，例如大量使用 `.Result`、`.GetAwaiter().GetResult()` 或 `.Wait()`？这会导致什么致命后果？
114. 为什么 `HttpClient` 不能频繁地在代码里 `using new HttpClient()`？这会引发什么底层网络问题？

### 🟡 进阶（中级）

115. `IHttpClientFactory` 是如何优雅地管理底层 `HttpMessageHandler` 生命周期并解决 Socket Exhaustion（Socket 耗尽漏洞）与 DNS Refresh（DNS 刷新失效）问题的？
116. `Typed Client`（强类型客户端）和 `Named Client`（命名客户端）的区别和使用场景是什么？
117. 什么是 `BackgroundService` 和 `IHostedService`？在 ASP.NET Core 中如何正确运行轻量级的后台定时任务或 Job？
118. 为什么不能直接在 Controller 的 Action 方法里通过 `new Thread()` 或直接调用 `Task.Run()` 来抛出后台任务？
119. 什么是 `DelegatingHandler`？如何利用它在 HttpClient 调用链中注入统一的日志、认证头，或者集成 Polly 实现 Retry Pattern（重试）和 Circuit Breaker（熔断）？
120. Response Compression（响应压缩）中间件有什么用？Gzip / Brotli 压缩对 API 性能有哪些正面和负面影响？
121. 什么是 Output Caching（输出缓存）和 Response Caching（响应缓存）？它们的工作层级和区别是什么？
122. 本地内存缓存 `IMemoryCache` 和分布式缓存 `IDistributedCache`（如 Redis）在可用性、一致性、伸缩性上的权衡（Trade-offs）是什么？

### 🔴 深度（高级）

123. **【异步底层机制】** 当一个异步 I/O 请求（如等待数据库返回或调用外部 API）被 `await` 时，Kestrel 工作线程（Worker Thread）去哪了？当 I/O 完成后，底层又是如何通过操作系统 I/O 完成端口（IOCP）通知线程池（Thread Pool）指派新线程继续往下执行的？
124. 什么是 Thread Pool Starvation（线程池饥饿）？它是怎么发生的？ASP.NET Core 内部的线程池注入和自动扩展（Scaling）机制是怎样的？
125. 深入高并发高可用缓存架构：在高吞吐 API 中，如何利用设计模式避免以下三个灾难性问题？

- **Cache Avalanche（缓存雪崩）** 的成因与防御。
- **Cache Breakdown（缓存击穿）** 的成因，以及如何通过 `SemaphoreSlim` 或分布式锁（Redis lock）实现 Single-flight / Cache Aside 串行加载。
- **Cache Penetration（缓存穿透）** 的成因与布隆过滤器（Bloom Filter）/ 空值缓存解决方案。

126. **【真实架构调优】** 如果一个生产环境的 ASP.NET Core API 突然响应时间超过 10 秒，或者 CPU / 内存突然暴涨，你会通过什么样的方法论和工具链（如 Prometheus, Grafana, OpenTelemetry 分布式链路追踪, Serilog 结构化日志）进行全链路定位与精准排查？
127. 从代码架构角度，为什么说 Controller 层绝不应该包含任何复杂的业务逻辑（Business Logic）？面对庞大的企业级应用，如何合理演进到三层架构、Repository Pattern（仓储模式）、Clean Architecture（整洁架构）或结合 MediatR 的 CQRS（读写分离架构）？
